# Normalize line endings so message files and code stay consistent across the
# different PCs on the channel (Windows/Linux/macOS).
* text=auto eol=lf
*.rs   text eol=lf
*.md   text eol=lf
*.toml text eol=lf
